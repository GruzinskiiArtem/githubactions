<?php
    $helper = $this->helper('Accord\Suppliers\Helper\Supplier');
    $supplier = $block->getSupplier();

    $coreHelper = $this->helper('\Magezon\Core\Helper\Data');
?>

<?php if (boolval($supplier->getStatus()) && $supplier->getContent()): ?>
    <?= $coreHelper->filter($supplier->getContent()) ?>
<?php endif; ?>
